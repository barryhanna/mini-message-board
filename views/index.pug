extends layout

block content
  h1= title
  p Welcome to #{title}
  ul
    each message in messages
      li
        span.username #{message.user}
        |&nbsp;on&nbsp;
        span.date-added #{message.added}:&nbsp;
        span #{message.text}

  a(href="/new", title="new message") New message
